<Window x:Class="SharpPasswordManager.Views.PasswordCheckView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SharpPasswordManager.Views"
        mc:Ignorable="d"
        Title="PasswordCheckView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Password"/>
                <TextBox Text="{Binding Path=Password, Mode=TwoWay}" Width="300" Margin="5 0 0 0"/>
            </StackPanel>
            <Button Command="{Binding CheckPasswordCmd}" Height="25" Width="100" Content="Ok" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
