<UserControl x:Class="SharpPasswordManager.Views.DataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SharpPasswordManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <StackPanel>
        <TextBlock Text="Data"/>
        <Button Content="Add" Command="{Binding AddDataCmd}"/>

        <ListBox x:Name="dataList" ItemsSource="{Binding DataList}" Height="100">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="5" Orientation="Horizontal">
                        <TextBlock FontSize="16" Text="{Binding Path=Description}"/>
                        <TextBlock FontSize="16" Text="{Binding Path=Login}"/>
                        <TextBlock FontSize="16" Text="{Binding Path=Password}"/>
                        <TextBlock FontSize="16" Text="{Binding Path=Date}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
    </StackPanel>
</UserControl>
