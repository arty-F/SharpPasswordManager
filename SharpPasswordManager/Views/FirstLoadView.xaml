<Window x:Class="SharpPasswordManager.Views.FirstLoadView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SharpPasswordManager.Views"
        mc:Ignorable="d"
        Title="FirstLoadView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock>Password</TextBlock>
                <TextBox Text="{Binding Path=Password, Mode=TwoWay}" Width="300" Margin="5 0 0 0"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock>Confirm</TextBlock>
                <TextBox Text="{Binding Path=ConfirmPassword, Mode=TwoWay}" Width="300" Margin="15 0 0 0"/>
            </StackPanel>

            <Button Margin="10" Width="100" Height="25" Content="Ok" Command="{Binding CreatePasswordCmd}"/>
        </StackPanel>

    </Grid>
</Window>
